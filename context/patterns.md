# Patterns

- Page components in `src/pages/` use Docusaurus `Layout`, `Heading`, and `@docusaurus/Link`.
- CSS modules are colocated with pages/components.
- Content-first: docs and blog are Markdown/MDX files in their folders.
